Task: Clean and refactor the codebase for efficiency.

Steps:

Move all unused files to temp/unused/ and log them in cleanup.md.

Document every change (file name + what/why) in cleanup.md. add comments to files

Remove all Bazaar components; replace with MUI (per MUI docs).

Delete dead code & merge duplicates into shared utils/hooks.

Organize components/ into subfolders by type.

Refactor to maximize component reuse.

Split any file > 350 lines.

Review every file — no skipping. super important!!!!

Run linter & test app — no broken imports or UI issues.