/**
 * UI Components Barrel Export
 * 
 * Centralized exports for all UI utility components.
 * TypeScript-ready with full Next.js compatibility.
 */

export { default as ErrorBoundary } from './ErrorBoundary';
export { default as OptimizedImage } from './OptimizedImage';
export { default as StyledButton } from './StyledButton';
export { default as SupabaseError } from './SupabaseError';

// Type exports for convenience
export type { default as ErrorBoundaryProps } from './ErrorBoundary';
